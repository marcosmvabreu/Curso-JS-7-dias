let clients = ['Marcos', 'Fabio', 'Matheus']

clients.sort(); //Organiza os itens do array de forma alfabética
clients.reverse(); //Organiza os itens do array de forma reversa, de tras pra frente
console.log(clients)